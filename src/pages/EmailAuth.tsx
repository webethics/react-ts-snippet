import React, { type ReactElement } from "react";
import SigninEmail from "../components/SigninEmail";

function EmailAuth(): ReactElement<void> {
    return <SigninEmail />;
}

export default EmailAuth;
